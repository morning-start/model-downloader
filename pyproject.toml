[project]
name = "model-downloader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "modelscope>=1.24.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.12.0",
]
